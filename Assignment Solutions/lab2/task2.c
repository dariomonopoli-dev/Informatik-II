
#include <stdio.h>

// hard-coded maximum length for input strings
const int MAX_LENGTH = 1000;

if (upper_limit == 0)
if (arr[0] < smallest){ //first element of the array is the smallest (so we return smallest)
    return smallest;
}
else if (arr[0] < second_smallest && arr[0] != smallest){ //it is smaller than the second smallest but is not the smallest (otherwise we return the smallest and not the sewcond smallest if we don't check the second condition)

return arr[0];
}
else{
    return second_smallest;
}

int main() {
	printf("Values of array separated by spaces (non-number to stop): ");
	int arr[MAX_LENGTH];
	int pos = 0;
	while (scanf("%d", &arr[pos]) == 1) {
		pos++;
	}
	// variable pos will contain number of integers read in from user

	// TODO: your implementation

	return 0;
}
